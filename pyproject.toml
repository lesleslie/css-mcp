[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "css-mcp"
version = "0.3.2"
description = "Universal CSS analysis and documentation MCP server with 150+ metrics"
readme = "README.md"
license = "BSD-3-Clause"
requires-python = ">=3.13"
authors = [
    { name = "Les Leslie", email = "les@lesleslie.com" }
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: BSD License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "fastmcp>=3.4.0,<4",
    "httpx~=0.28.1",
    "mcp-common>=0.17.0",
    "oneiric>=0.16.0",
    "pydantic~=2.13.4",
    "tinycss2~=1.5.1",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.9.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.4.0",
]

[project.scripts]
css-mcp = "css_mcp.cli:main"

[project.urls]
Homepage = "https://github.com/lesleslie/css-mcp"
Repository = "https://github.com/lesleslie/css-mcp"

[tool.hatch.build.targets.wheel]
packages = ["css_mcp"]

[tool.hatch.build.targets.sdist]
exclude = [
    "/.crackerjack",
    "/.cache",
    "/.venv",
    "/.pytest_cache",
    "/.mypy_cache",
    "/.ruff_cache",
    "/.oneiric_cache",
    "/.benchmarks",
    "/.pyscn",
    "/tests",
    "/dist",
    "/settings/local.yaml",
    "/.lycheecache",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM", "TCH"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.uv.sources]
mcp-common = { path = "../mcp-common", editable = true }

[tool.uv]
constraint-dependencies = [
    "aiohttp>=3.14.3",
    "cryptography>=50.0.0",
]

[dependency-groups]
dev = [
    "crackerjack>=0.54.3",
    "session-buddy>=0.14.3",
]
